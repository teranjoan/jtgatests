name: Build Action
outputs:
  image:
    description: "Generated image tag"
    value: ${{ steps.build-step.outputs.image }}
runs:
  using: "composite"
  steps:
    - id: build-step
      run: echo "image=jtga-builded-image:latest" >> $GITHUB_OUTPUT
